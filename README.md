To use the project, create an api key from Google Ai Studio. Then create a .env file in the server folder and import the api key in that.
Rest, only npm install for backend (server folder), and download any other dependencies for the project. 
